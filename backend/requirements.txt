fastapi>=0.100.0
uvicorn>=0.20.0
sqlalchemy>=2.0.0
pydantic>=2.0.0
python-multipart>=0.0.6
pymupdf>=1.22.0
python-docx>=0.8.11

# --- Machine Learning (opsional untuk inferensi) ---
# Backend tetap berjalan tanpa paket ini menggunakan fallback rule-based.
# Pasang ini hanya jika kamu ingin menjalankan model hasil training.
# joblib + scikit-learn ringan (untuk classifier). torch + transformers berat
# (untuk NMT) — gunakan jika sudah punya artefak model di folder models/.
scikit-learn>=1.3.0
joblib>=1.3.0
# torch>=2.0.0
# transformers>=4.40.0
# sentencepiece>=0.1.99
